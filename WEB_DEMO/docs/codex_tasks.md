# Codex Tasks

---

# 当前任务：WEB_DEMO v0.8-audit-fix 黑影攻击弓箭手范围偏差与围墙通行规则确认

## 任务背景

WEB_DEMO v0.8-refactor 已完成并通过静态代码审核，核心内容包括：

```text
1. 墙基
2. 派工建墙
3. 围墙 HP
4. 黑影攻击围墙
5. 黑影目标优先级扩展
6. 弓箭手夜晚自动瞄准黑影
7. 弓箭手攻击黑影
8. 弓箭手冷却
9. 防御相关 HUD 和表现
```

审核结论：

```text
WEB_DEMO v0.8-refactor 基本通过。
```

但审核发现两个需要在进入 v0.9 前同步记录的问题：

```text
1. 当前代码已实现黑影攻击弓箭手 / 弓箭手 lost，这比 v0.8 任务卡原本的禁止范围略超前。
2. 当前 WALL 仍然是可通行地块，围墙是否应该阻挡移动仍需要策划确认。
```

本轮是小修复任务，主要目标是同步审计文档和 known issues，使代码、任务范围和审计记录一致。

---

## 本轮目标

本轮只做文档同步，不做新功能。

需要明确记录：

```text
1. 黑影攻击弓箭手当前已经在 v0.8 中实现。
2. 该实现属于“有意超前实现 / 待策划确认”，不是 v0.8 任务核心目标。
3. 如果后续决定保留，则正式纳入黑影目标规则。
4. 如果后续决定不保留，则应在后续版本移除 hitArcher 和 ARCHER 目标伤害分支。
5. WALL 当前保持可通行，是否改为阻挡需要策划确认。
```

---

# 允许修改

```text
WEB_DEMO/docs/codex_tasks.md
WEB_DEMO/docs/known_issues.md
WEB_DEMO/design/audit/gpt_to_web_rule_audit_v1.md
WEB_DEMO/docs/changelog.md
```

---

# 禁止修改

```text
WEB_DEMO/src/**
GPT_DEMO/**
AI_RULES/**
DESIGN_HUB/**
AI_TASKS/**
根目录 README.md
PROJECT_STATUS.md
```

---

# 必须修正 1：审计文档中记录黑影攻击弓箭手范围偏差

文件：

```text
WEB_DEMO/design/audit/gpt_to_web_rule_audit_v1.md
```

需要在 v0.8 相关审计记录中明确增加：

```text
v0.8 中已经实现黑影将弓箭手作为局部目标，并在接触后使弓箭手 lost。
该行为相对 v0.8 原任务卡属于范围轻微超前。
由于 GPT_DEMO 黑影目标优先级包含弓箭手，因此当前实现可以暂时保留，但必须标记为待策划确认。
```

如果审计表中已有“黑影目标优先级”行，需要将备注修正为类似：

```text
已实现辉石 > 围墙 > 工人 > 弓箭手 > 玩家 > 家园推进；返程流民目标后置。v0.8 已额外实现黑影接触弓箭手后使其 lost，属于相对任务卡的超前实现，待策划确认是否保留。
```

如果审计表中已有“弓箭手”行，需要将备注修正为类似：

```text
已实现夜晚自动瞄准、1 点伤害、击杀 HP 1 黑影和 2.2 秒冷却；同时 v0.8 代码已实现弓箭手可被黑影拖走 lost，该部分属于超前实现，待策划确认。
```

---

# 必须修正 2：known_issues 记录两个待确认问题

文件：

```text
WEB_DEMO/docs/known_issues.md
```

必须新增或更新：

```markdown
## v0.8-audit-fix 待确认问题

### 黑影攻击弓箭手是否正式保留

v0.8 代码已经实现黑影把弓箭手作为局部目标，并在接触后使弓箭手进入 lost 状态。
该行为与 v0.8 原任务卡“不做黑影攻击弓箭手”的限制存在轻微范围偏差。
由于 GPT_DEMO 的黑影目标优先级包含弓箭手，当前实现暂时保留为“有意超前实现 / 待策划确认”。
后续需要确认：
- 保留：正式纳入黑影目标规则。
- 移除：删除 ARCHER 目标伤害分支和 hitArcher。

### 围墙是否应该可通行

当前 WEB_DEMO 中 WALL 仍然是可通行地块。
这可能继承了 GPT_DEMO 当前表现，但从防线直觉看，围墙也可能应该阻挡玩家、工人和黑影移动。
后续需要确认：
- 继承 GPT_DEMO：围墙可通行，只作为黑影目标 / HP 防御点。
- 改为正式规则：围墙不可通行，成为真正地形阻挡；若这样做，应标记为有意重设计。
```

---

# 必须修正 3：changelog 记录 v0.8-audit-fix

文件：

```text
WEB_DEMO/docs/changelog.md
```

需要新增：

```markdown
## v0.8-audit-fix

状态：审计同步修复。

内容：
- 记录 v0.8 中黑影攻击弓箭手 / 弓箭手 lost 属于相对任务卡的范围轻微超前。
- 暂时将该行为标记为有意超前实现 / 待策划确认。
- 记录 WALL 当前仍为可通行地块，围墙是否应作为地形阻挡待策划确认。
- 本轮不修改 WEB_DEMO/src 代码，不修改 GPT_DEMO。
```

---

# 本轮不要做

禁止本轮实现或修改：

```text
1. 不修改 WEB_DEMO/src/**。
2. 不移除 hitArcher。
3. 不修改 MonsterSystem。
4. 不修改 TileMap。
5. 不把 WALL 改成不可通行。
6. 不做围墙升级。
7. 不做营地损坏。
8. 不做黑影攻击返程流民。
9. 不做特殊事件。
10. 不做小地图。
11. 不修改 GPT_DEMO。
```

---

# 验收标准

必须满足：

```text
1. known_issues.md 记录“黑影攻击弓箭手是否正式保留”的待确认问题。
2. known_issues.md 记录“围墙是否应该可通行”的待确认问题。
3. gpt_to_web_rule_audit_v1.md 记录 v0.8 已实现黑影攻击弓箭手 / 弓箭手 lost，且标记为超前实现 / 待策划确认。
4. changelog.md 新增 v0.8-audit-fix 记录。
5. 本轮没有修改 WEB_DEMO/src/**。
6. 本轮没有修改 GPT_DEMO/**。
```

---

# 完成后的输出要求

完成后请输出：

```text
1. 修改了哪些文档。
2. 是否修改了 WEB_DEMO/src/**。
3. 是否修改了 GPT_DEMO/**。
4. 黑影攻击弓箭手是否已记录为超前实现 / 待确认。
5. 围墙通行规则是否已记录为待确认。
6. 是否可以进入 v0.9-refactor。
```
